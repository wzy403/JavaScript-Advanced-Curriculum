/**
* Shuffle an array in place
* @param {Array} array - The array to shuffle.
* @returns {Array} The original array, shuffled in place.
*/
export default function shuffle(array) {
 // TODO: Explore your own shuffling algorithm.

 return array;
}
