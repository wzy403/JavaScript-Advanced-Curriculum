const output = document.getElementById("output");

document.getElementById("btn").addEventListener("click", async function () {
  // TODO: Fetch data from the GitHub API for the user 'octocat',
  //       check if the response is ok, if not, throw an error
  //       with the status, parse the response as JSON, display
  //       the JSON data in the 'output' element, handle any errors
  //       that occur during the fetch operation
  //
  // NOTE: This time use async/await instead of promises.
});
